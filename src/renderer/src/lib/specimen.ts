/**
 * The sample every specimen starts from: the Fonts toolbar's text and size, the
 * type scale screens' preview, and the fallback when a caller passes an empty
 * string.
 *
 * Stated once because it was stated seven times and disagreed: three screens
 * started from the full pangram at 20px, four fell back to the short phrase,
 * and the Figma build had drawn 24px and the short phrase from the start. The
 * design is the reference, so the code takes its values.
 */
export const SPECIMEN_TEXT = 'The quick brown fox'
export const SPECIMEN_SIZE = 24
